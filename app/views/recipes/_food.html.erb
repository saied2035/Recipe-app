<%# <div id="<%= dom_id food %>
<td><%= food.name %></td>
<%# #unit %>
<td><%= "#{food.quantity} #{food.unit}" %></td>
<%# #price %>
<td><%= "$#{food.price * food.quantity}"  %></td>
<%# #action %>
<% if can? :manage, food%>
<% if params[:id] %>
<td class="action_btn">
   <%recipe_food = RecipeFood.find_by(recipe_id: recipe.id, food_id: food.id)%>
  <%= button_to "Delete", recipe_recipe_food_url(id:recipe_food.id,food_id:food.id,recipe_id:recipe.id), class: "table_delete_btn", 
  method: :delete %>
  <%= link_to "Modify", edit_food_path(id:food.id,recipe_id:recipe.id), class: "table_modify_btn"%>  
</td>
<%end%>
<%end%>
<%# </div> %>
