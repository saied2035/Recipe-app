<%= form_with model: recipe_food, url: recipe_recipe_foods_url, method: :post do |form| %>
  <% if recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_food.errors.count, "error") %> prohibited this recipe food from being saved:</h2>

      <ul>
        <% recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.collection_select :food_id, Food.order(:created_at),:id,:name, include_blank: true %>
    <%=form.number_field :quantity, placeholder:"Quantity"%>
    <%= form.submit 'Add'%>
  </div>
<% end %>
